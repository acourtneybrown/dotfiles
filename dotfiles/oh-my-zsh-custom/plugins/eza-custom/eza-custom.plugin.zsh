# shellcheck disable=SC2148
# Override `ls` aliases from oh-my-zsh/lib/directories.zsh to eza equivalent

alias lsa='ls -laa'
alias l='ls -laa'
# ls alias already present in eza oh-my-zsh plugin
# la alias already present in eza oh-my-zsh plugin
