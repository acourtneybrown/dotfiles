# Switch to ZSH (if we can't chsh)
command -v zsh >/dev/null && exec zsh

# Added by LM Studio CLI (lms)
export PATH="$PATH:/Users/adambrown/.lmstudio/bin"
# End of LM Studio CLI section


# Added by LM Studio CLI (lms)
export PATH="$PATH:/Users/adambrown/.lmstudio/bin"
# End of LM Studio CLI section

